{
  "name": "adapta-summit-leads",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "App de captacao de contatos para eventos — pre-cadastro via QR Code, painel e exportacao.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "test": "node --test \"tests/*.test.js\""
  }
}
